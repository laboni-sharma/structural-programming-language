#include <stdio.h>
int main()
{
    int length = 4;
    int width = 6;
    int area;
    area = length*width;
    printf("length is: %d\n", length);
    printf("width is : %d\n", width);
    printf("the area of rectangle is : %g\n",area);
    return 0;
}